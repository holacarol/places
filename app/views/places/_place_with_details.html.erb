<div class="model_with_details">
  <div class="logo">
    <%= link_to (image_tag("place50x50.png" , :size => "48x48", :alt => place.title )), place %>
  </div>
  <div class="sub-block">
    <div class="name black" >
      <%= link_to truncate_name(place.title), place %>
    </div>
    <div class="brief">
      <%= place.address.locality %>, <%= place.address.country %>
      <%# If we want like count %>
      <%# <%= t 'like.n', :count => place.like_count %>
    </div>
   </div>
</div>